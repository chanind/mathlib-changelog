import type { NextPage } from "next";
import Layout from "../components/Layout";

const Home: NextPage = () => {
  return (
    <Layout>
      <div className="flex flex-wrap">Check it!</div>
    </Layout>
  );
};

export default Home;
